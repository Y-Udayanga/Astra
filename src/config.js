// Public site URL used for OAuth redirects and PayHere return/cancel/notify URLs.
// Always points at the deployed Vercel app (no trailing slash).
export const SITE_URL = 'https://astra-web-app.vercel.app';
